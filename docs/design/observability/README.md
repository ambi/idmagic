# オブザーバビリティ設計

## 目的

この文書は、システム共通のオブザーバビリティに必要な信号、相関、所有境界を定める。個別のメトリクス、ログ、トレースは子文書が所有し、サービス目標の値と運用手順はここへ複製しない。

## 信号モデル

| 信号 | 所有者 | 主な用途 |
| --- | --- | --- |
| Metrics | [監視設計](monitoring.md) | サービス目標、資源、依存先、キューの継続的な状態判定とアラート |
| Logs | [ログ設計](logging.md) | 個別リクエストとジョブの事実、障害理由、監査対象外の診断情報 |
| Traces | [トレース設計](tracing.md) | 実行単位と外部依存をまたぐ因果関係と所要時間 |

監査イベントは診断ログではなく、改ざん耐性と保持要件を持つ業務上の記録である。監査の意味と保持期間は [Audit](../../contexts/audit/README.md) が所有し、監査イベントがほかのデータとどう違う消え方をするかは[データライフサイクル設計](../data/lifecycle.md)が持つ。

## 相関

API は入口で `traceparent` を検証し、有効ならトレースを継続し、無効または欠落なら新しいトレースを開始する。すべてのレスポンスへ `X-Request-ID` を返し、構造化ログには `request_id`、`trace_id`、登録済みのルートパターン、メソッド、状態コード、所要時間を含める。Worker と Batch はジョブまたは実行の識別子を持ち、HTTP リクエストから派生した処理では元の相関情報を伝播する。

テナント ID、ユーザー ID、クライアント ID などの高カーディナリティ値をメトリクスのラベルにしない。必要な調査軸はログまたはトレースに置き、資格情報、トークン、Cookie、認可コード、秘密鍵、復号済みの秘密情報をいずれの信号にも記録しない。

## 所有と流れ

アプリケーションは OpenTelemetry と構造化ログを生成し、Prometheus はメトリクスを収集する。現行のローカル監視構成では Grafana Alloy がコンテナログを Loki へ送り、Grafana が Prometheus と Loki を表示する。プロダクトコードは特定の可視化プロダクトへ依存せず、信号の意味と属性名を安定させる。

サービス目標の測定境界と値は [品質要求](../../requirements/quality.md) が、アラートからの対応手順は [Runbooks](../../runbooks/) が所有する。観測設計は、目標を計算できる信号と手順へ到達できる通知を提供する。

## 子文書の担当

| 子文書 | 定めるもの |
| --- | --- |
| [監視設計](monitoring.md) | 検知したいことと、それを判定する信号、通知の種類、対応する runbook の対応。検知できていないものの記録 |
| [ログ設計](logging.md) | 共通フィールド、`event_name` の命名規約、重大度、サンプリング規則、保持と閲覧権限、容量超過と収集停止時の動作 |
| [トレース設計](tracing.md) | 伝播、標本化、属性と span 名、保持とコスト。実装の現状と目標設計の区別 |

## 未確定事項

デプロイ先が決まるまで確定しない項目がある。
値が決まるまで、これらを暗黙の保証として扱わない。

| 未確定な項目 | 記録する子文書 |
| --- | --- |
| 通知の宛先とエスカレーション | [監視設計](monitoring.md) |
| ログの保持期間、外部オブジェクトストレージ、閲覧権限の分離 | [ログ設計](logging.md) |
| トレースの保存先、本番の標本化率、保持期間 | [トレース設計](tracing.md) |

いずれの信号についても、**欠落を正常と読まない**。
アラートが鳴らないこと、ダッシュボードが空であること、トレースが見つからないことは、収集が止まっている場合にも同じ見え方をする。
収集そのものの状態は、[監視設計](monitoring.md)が別の判定として持つ。
