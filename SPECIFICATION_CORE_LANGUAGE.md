# Specification Core Language (retired)

The project-specific SCL YAML dialect was retired by `wi-355`. Current specification rules are in [SPECIFICATION_FORMAT.md](SPECIFICATION_FORMAT.md). This stub remains only so historical work items and ADR links do not break.
