---
status: pending
authors: [tn]
risk: medium
reversibility: reversible
created_at: 2026-09-24
priority: p2
depends_on: []
change_kind: bugfix
affected_spec:
  - { path: docs/domain/identity-management/scenarios.feature.md, requirement: REQ-IDMANAGEMENT-013 }
  - { path: docs/domain/tenancy/scenarios.feature.md, requirement: REQ-TENANCY-013 }
---

# User の完全削除を再実行で完了できるようにし、クォータの使用量を再集計で補正する

## 動機

[[wi-22350-capture-provisioning-deliveries-in-the-mutation-transaction]] の調査で、複数の書き込みを別々に確定している箇所が 2 つ見つかった。
どちらも途中で失敗すると不整合が残り、しかも回復する手段がない。
トランザクションで囲めば防げるが、次の理由でその方式は採らず、再実行と照合で回収する方針とした。

- Valkey に置くセッションなど、同一 DB の外の状態は囲めない。
- 囲む範囲を経路ごとに正しく保つ前提は、経路が増えるたびに破れる。

| 箇所 | 現状 | 失敗したときに残る状態 |
| --- | --- | --- |
| User の完全削除 | `admin_users.go` の `DeleteUser` が、匿名化した User を保存してから、Consent、RefreshToken、セッション、パスワード履歴、MFA 要素、信頼済み端末、WebAuthn の資格情報、復旧コードを 1 つずつ削除し、最後にクォータを減らす | 削除済みの User に紐づく記録が残る。`DeleteUser` は削除済みの User を見て何もせずに返るため、再実行してもカスケードが走らない |
| クォータの加算 | `CreateUser` と `ProvisionFederatedUser` が、User の保存より先に使用量を加算して確定する | User が存在しないのに使用量だけが増えたまま残り、テナントが上限に早く達する。再集計の仕組みはない |

さらに `docs/domain/identity-management/internals.md` は「PostgreSQL を使うカスケード処理は 1 つのトランザクション内で行う」と書いており、実装と食い違っている。

## 対象範囲

- `DeleteUser` を、User がすでに削除済みでもカスケードとクォータの補正を再実行する形にする。各カスケードの削除は冪等なので、再実行しても結果は変わらない。監査イベント `UserDeleted` は最初の削除でだけ発行する。
- クォータの使用量を実数から数え直して補正する再集計を実装し、定期ジョブとして走らせる。
- `docs/domain/identity-management/internals.md` のカスケードの記述を、トランザクションではなく再実行で完了させる設計へ改める。

## 対象外

- 猶予期間を過ぎた削除予約の自動完全削除（lazy-on-access）の経路の見直し。同じ `DeleteUser` を通るなら本項目の変更がそのまま効く。
- User 以外のリソース（Group、Application など）のクォータの再集計。同じ形で広げられるが、まず User で確かめる。
- 途中で失敗した削除を自動で再実行する仕組み。まずは管理者が同じ削除をもう一度実行すれば完了する状態にする。

## 設計

### 削除の再実行

`DeleteUser` の早期 return を、削除済みかどうかではなく「カスケードまで完了したかどうか」で判定する形にはしない。
完了の記録を別に持つと、それ自体が別の書き込みになり、同じ問題が再び起きる。
代わりに、削除済みでもカスケードを毎回実行する。
カスケードの各操作は「その `sub` の記録をすべて消す」なので冪等であり、2 回目以降は何も消さずに終わる。

削除済みの User に対するクォータの減算は、再実行のたびに行うと二重に減る。
そのため減算はカスケードから外し、使用量は再集計で実数へ合わせる。

### クォータの再集計

使用量 `tenant_usages` はリソース作成のたびに全件を数えないための集計である（`docs/design/data/database.md`）。
集計が実数からずれても、定期的に実数から数え直して上書きすれば、ずれは次の周期で消える。
作成時の加算は、上限の判定のための近道として残す。

### 採用しない代替案

- **カスケードとクォータの更新を User の保存と同じトランザクションに入れる。** Valkey のセッションは入らない。wi-22350 で試した作用範囲の仕組みを全保存先へ入れる必要があり、仕組みの量に見合わない。
- **User の保存に失敗したらクォータを減らし戻す補償処理。** 補償の書き込み自体が失敗し得るうえ、プロセスの異常終了では補償が走らない。

## 計画

着手時に次の問いを解決する。どれも作るものを変える。

1. 再集計の周期と、どのプロセスで走らせるか（`worker` の定期ジョブか、`idmagic-batch` のコマンドか）。
2. 再集計中に並行して作成があったときの扱い。数え直しと上書きの間に加算された分を失わない書き方（差分での補正など）を選ぶ。
3. `REQ-IDMANAGEMENT-013` に「途中で失敗した完全削除は、同じ操作をもう一度実行すれば完了する」という具体例を足すか。

## タスク

- [ ] T001 [Spec] 必要なら `REQ-IDMANAGEMENT-013` に再実行の具体例を足し、`mise run check-spec` を通す。
- [ ] T002 [Acceptance] カスケードの途中で失敗した完全削除を再実行しても、残った記録が消えないことを RED として観測する。
- [ ] T003 [App] 削除済みでもカスケードを再実行するようにする。
- [ ] T004 [Acceptance] User の保存に失敗した作成で使用量だけが増えることを RED として観測する。
- [ ] T005 [App] クォータの使用量の再集計を実装し、定期ジョブとして組み立てる。
- [ ] T006 [Docs] IdManagement の内部設計と、設計ガイドラインのトランザクションの類型を現状に合わせる。
- [ ] T007 [Verify] 変更を検証する。

## 検証

- `mise run check-spec`
- `mise run test-go-changed`
- `mise run verify`

## リスク

- **再実行で監査イベントが二重に発行される。** `UserDeleted` は最初の削除でだけ発行し、再実行では発行しないことをテストで固定する。
- **再集計が並行する作成の加算を上書きで失う。** 計画の問い 2 で書き方を決め、並行する作成を挟んだテストで確かめる。
- **設計ガイドラインの記述と食い違う。** `docs/design/application/design-guidelines.md` は削除のカスケードを「1 つのトランザクションで変更してよい類型」として挙げている。本項目で記述を改める。
