// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: mfa_enrollment_bypasses.sql

package db_postgres

import (
	"context"
	"time"

	"github.com/jackc/pgx/v5/pgtype"
)

const consumeActiveMfaEnrollmentBypass = `-- name: ConsumeActiveMfaEnrollmentBypass :one
UPDATE mfa_enrollment_bypasses
SET consumed_at = $3
WHERE id = (
    SELECT b.id FROM mfa_enrollment_bypasses b
    WHERE b.tenant_id = $1 AND b.user_id = $2 AND b.consumed_at IS NULL AND b.revoked_at IS NULL AND b.expired_at IS NULL AND b.expires_at > $3
    ORDER BY b.issued_at DESC LIMIT 1 FOR UPDATE SKIP LOCKED
)
RETURNING id, tenant_id, user_id, issued_by, issued_at, expires_at, consumed_at, revoked_at, expired_at
`

type ConsumeActiveMfaEnrollmentBypassParams struct {
	TenantID   string
	UserID     string
	ConsumedAt pgtype.Timestamptz
}

func (q *Queries) ConsumeActiveMfaEnrollmentBypass(ctx context.Context, arg ConsumeActiveMfaEnrollmentBypassParams) (*MfaEnrollmentBypass, error) {
	row := q.db.QueryRow(ctx, consumeActiveMfaEnrollmentBypass, arg.TenantID, arg.UserID, arg.ConsumedAt)
	var i MfaEnrollmentBypass
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.UserID,
		&i.IssuedBy,
		&i.IssuedAt,
		&i.ExpiresAt,
		&i.ConsumedAt,
		&i.RevokedAt,
		&i.ExpiredAt,
	)
	return &i, err
}

const expireOpenMfaEnrollmentBypass = `-- name: ExpireOpenMfaEnrollmentBypass :one
UPDATE mfa_enrollment_bypasses
SET expired_at = $3
WHERE id = (
    SELECT b.id FROM mfa_enrollment_bypasses b
    WHERE b.tenant_id = $1 AND b.user_id = $2 AND b.consumed_at IS NULL AND b.revoked_at IS NULL AND b.expired_at IS NULL AND b.expires_at <= $3
    ORDER BY b.issued_at DESC LIMIT 1 FOR UPDATE SKIP LOCKED
)
RETURNING id, tenant_id, user_id, issued_by, issued_at, expires_at, consumed_at, revoked_at, expired_at
`

type ExpireOpenMfaEnrollmentBypassParams struct {
	TenantID  string
	UserID    string
	ExpiredAt pgtype.Timestamptz
}

func (q *Queries) ExpireOpenMfaEnrollmentBypass(ctx context.Context, arg ExpireOpenMfaEnrollmentBypassParams) (*MfaEnrollmentBypass, error) {
	row := q.db.QueryRow(ctx, expireOpenMfaEnrollmentBypass, arg.TenantID, arg.UserID, arg.ExpiredAt)
	var i MfaEnrollmentBypass
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.UserID,
		&i.IssuedBy,
		&i.IssuedAt,
		&i.ExpiresAt,
		&i.ConsumedAt,
		&i.RevokedAt,
		&i.ExpiredAt,
	)
	return &i, err
}

const findActiveMfaEnrollmentBypass = `-- name: FindActiveMfaEnrollmentBypass :one
SELECT id, tenant_id, user_id, issued_by, issued_at, expires_at, consumed_at, revoked_at, expired_at
FROM mfa_enrollment_bypasses
WHERE tenant_id = $1 AND user_id = $2 AND consumed_at IS NULL AND revoked_at IS NULL AND expired_at IS NULL AND expires_at > $3
ORDER BY issued_at DESC LIMIT 1
`

type FindActiveMfaEnrollmentBypassParams struct {
	TenantID  string
	UserID    string
	ExpiresAt time.Time
}

func (q *Queries) FindActiveMfaEnrollmentBypass(ctx context.Context, arg FindActiveMfaEnrollmentBypassParams) (*MfaEnrollmentBypass, error) {
	row := q.db.QueryRow(ctx, findActiveMfaEnrollmentBypass, arg.TenantID, arg.UserID, arg.ExpiresAt)
	var i MfaEnrollmentBypass
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.UserID,
		&i.IssuedBy,
		&i.IssuedAt,
		&i.ExpiresAt,
		&i.ConsumedAt,
		&i.RevokedAt,
		&i.ExpiredAt,
	)
	return &i, err
}

const revokeActiveMfaEnrollmentBypass = `-- name: RevokeActiveMfaEnrollmentBypass :one
UPDATE mfa_enrollment_bypasses
SET revoked_at = $3
WHERE id = (
    SELECT b.id FROM mfa_enrollment_bypasses b
    WHERE b.tenant_id = $1 AND b.user_id = $2 AND b.consumed_at IS NULL AND b.revoked_at IS NULL AND b.expired_at IS NULL
    ORDER BY b.issued_at DESC LIMIT 1 FOR UPDATE SKIP LOCKED
)
RETURNING id, tenant_id, user_id, issued_by, issued_at, expires_at, consumed_at, revoked_at, expired_at
`

type RevokeActiveMfaEnrollmentBypassParams struct {
	TenantID  string
	UserID    string
	RevokedAt pgtype.Timestamptz
}

func (q *Queries) RevokeActiveMfaEnrollmentBypass(ctx context.Context, arg RevokeActiveMfaEnrollmentBypassParams) (*MfaEnrollmentBypass, error) {
	row := q.db.QueryRow(ctx, revokeActiveMfaEnrollmentBypass, arg.TenantID, arg.UserID, arg.RevokedAt)
	var i MfaEnrollmentBypass
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.UserID,
		&i.IssuedBy,
		&i.IssuedAt,
		&i.ExpiresAt,
		&i.ConsumedAt,
		&i.RevokedAt,
		&i.ExpiredAt,
	)
	return &i, err
}

const saveMfaEnrollmentBypass = `-- name: SaveMfaEnrollmentBypass :exec
INSERT INTO mfa_enrollment_bypasses (id, tenant_id, user_id, issued_by, issued_at, expires_at)
VALUES ($1, $2, $3, $4, $5, $6)
`

type SaveMfaEnrollmentBypassParams struct {
	ID        string
	TenantID  string
	UserID    string
	IssuedBy  string
	IssuedAt  time.Time
	ExpiresAt time.Time
}

func (q *Queries) SaveMfaEnrollmentBypass(ctx context.Context, arg SaveMfaEnrollmentBypassParams) error {
	_, err := q.db.Exec(ctx, saveMfaEnrollmentBypass,
		arg.ID,
		arg.TenantID,
		arg.UserID,
		arg.IssuedBy,
		arg.IssuedAt,
		arg.ExpiresAt,
	)
	return err
}
